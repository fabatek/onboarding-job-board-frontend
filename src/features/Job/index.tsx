import React from 'react';

function JobFeature() {
    return (
        <div>
            /* 
                This components is for page routing.
                Not use yet because we only have 1 page: JobList
            */
        </div>
    );
}

export default JobFeature;